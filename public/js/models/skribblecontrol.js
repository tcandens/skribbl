define([
  'backbone'
], function( Backbone ) {
  'use strict';

  var SkribbleControlModel = Backbone.Model.extend({
    //defaults: {
      //'content': 'Skribble Data',
      //'parent': null,
      //'children': []
    //}
  });

  return SkribbleControlModel;
});
